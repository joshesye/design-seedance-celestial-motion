# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a practical Chinese README that documents installation, invocation, capabilities, output, repository structure, and verification.

**Architecture:** Keep all public onboarding content in one root `README.md`. Use a small shell contract test to ensure required sections and canonical identifiers remain present.

**Tech Stack:** Markdown, POSIX shell, `rg`.

## Global Constraints

- Document only behavior present in `SKILL.md` and `references/output-contract.md`.
- Use `design-seedance-celestial-motion` as the canonical Skill and directory name.
- Do not claim a license, version, screenshot, or compatibility that the repository does not provide.

---

### Task 1: Add README contract test

**Files:**
- Create: `tests/check-readme.sh`
- Test: `tests/check-readme.sh`

- [ ] Add assertions for non-empty README, canonical repository URL, Skill name, required headings, install path, and verification command.
- [ ] Run `sh tests/check-readme.sh` and verify it fails because `README.md` is absent.

### Task 2: Write and publish README

**Files:**
- Create: `README.md`

- [ ] Write the approved Chinese README sections using current project facts.
- [ ] Run `sh tests/check-readme.sh` and `sh tests/check-dynamic-contract.sh`; expect both to print `PASS`.
- [ ] Review Markdown links and shell commands, then commit and push `main`.
